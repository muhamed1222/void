// Theme constants

// Transition durations
export const TRANSITION_DURATIONS = {
  fast: 150,
  normal: 300,
  slow: 500,
};

// Animation curves
export const EASING_FUNCTIONS = {
  easeInOut: 'ease-in-out',
  easeOut: 'ease-out',
  easeIn: 'ease-in',
  linear: 'linear',
};

// Breakpoints
export const BREAKPOINTS = {
  mobile: 0,
  tablet: 768,
  desktop: 1024,
};

// Responsive multipliers
export const RESPONSIVE_MULTIPLIERS = {
  mobile: 1,
  tablet: 1.25,
  desktop: 1.5,
};

// Common icon sizes
export const ICON_SIZES = {
  xs: 12,
  sm: 16,
  md: 24,
  lg: 32,
  xl: 48,
};

// Common button sizes
export const BUTTON_SIZES = {
  sm: {
    height: 32,
    paddingVertical: 4,
    paddingHorizontal: 12,
    fontSize: 14,
  },
  md: {
    height: 44,
    paddingVertical: 8,
    paddingHorizontal: 16,
    fontSize: 16,
  },
  lg: {
    height: 56,
    paddingVertical: 12,
    paddingHorizontal: 24,
    fontSize: 18,
  },
};

// Common input sizes
export const INPUT_SIZES = {
  sm: {
    height: 32,
    paddingVertical: 4,
    paddingHorizontal: 8,
    fontSize: 14,
  },
  md: {
    height: 44,
    paddingVertical: 8,
    paddingHorizontal: 12,
    fontSize: 16,
  },
  lg: {
    height: 56,
    paddingVertical: 12,
    paddingHorizontal: 16,
    fontSize: 18,
  },
};

// Common card styles
export const CARD_STYLES = {
  elevated: {
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 2,
    },
    shadowOpacity: 0.1,
    shadowRadius: 4,
    elevation: 3,
  },
  outlined: {
    borderWidth: 1,
    borderColor: '#e0e0e0',
  },
  filled: {
    backgroundColor: '#f5f5f5',
  },
};

// Common avatar sizes
export const AVATAR_SIZES = {
  xs: 24,
  sm: 32,
  md: 48,
  lg: 64,
  xl: 96,
};

// Common badge sizes
export const BADGE_SIZES = {
  sm: {
    minWidth: 16,
    height: 16,
    borderRadius: 8,
    fontSize: 10,
  },
  md: {
    minWidth: 24,
    height: 24,
    borderRadius: 12,
    fontSize: 12,
  },
  lg: {
    minWidth: 32,
    height: 32,
    borderRadius: 16,
    fontSize: 14,
  },
};

// Common chip sizes
export const CHIP_SIZES = {
  sm: {
    height: 24,
    borderRadius: 12,
    paddingHorizontal: 8,
    fontSize: 12,
  },
  md: {
    height: 32,
    borderRadius: 16,
    paddingHorizontal: 12,
    fontSize: 14,
  },
  lg: {
    height: 40,
    borderRadius: 20,
    paddingHorizontal: 16,
    fontSize: 16,
  },
};

// Common list item heights
export const LIST_ITEM_HEIGHTS = {
  sm: 40,
  md: 56,
  lg: 72,
};

// Common header heights
export const HEADER_HEIGHTS = {
  sm: 48,
  md: 56,
  lg: 64,
};

// Common footer heights
export const FOOTER_HEIGHTS = {
  sm: 48,
  md: 56,
  lg: 64,
};

// Common navigation bar heights
export const NAVIGATION_BAR_HEIGHTS = {
  sm: 48,
  md: 56,
  lg: 64,
};

// Common tab bar heights
export const TAB_BAR_HEIGHTS = {
  sm: 48,
  md: 56,
  lg: 64,
};

// Common modal sizes
export const MODAL_SIZES = {
  sm: {
    width: '80%',
    maxWidth: 320,
  },
  md: {
    width: '90%',
    maxWidth: 480,
  },
  lg: {
    width: '95%',
    maxWidth: 640,
  },
  full: {
    width: '100%',
    height: '100%',
  },
};

// Common dialog sizes
export const DIALOG_SIZES = {
  sm: {
    width: 280,
  },
  md: {
    width: 320,
  },
  lg: {
    width: 360,
  },
};

// Common tooltip sizes
export const TOOLTIP_SIZES = {
  sm: {
    maxWidth: 120,
    padding: 8,
    fontSize: 12,
  },
  md: {
    maxWidth: 160,
    padding: 12,
    fontSize: 14,
  },
  lg: {
    maxWidth: 200,
    padding: 16,
    fontSize: 16,
  },
};